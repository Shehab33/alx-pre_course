#include <stdio.h>
/**
 * mul - is Fn that mulitiplies 2-num
 * @a: is the first num
 * @b: is the second num
 * Return: the result of multiplies
 */
int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
