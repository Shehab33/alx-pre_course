#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen - Fn that returns the length of string
 * @s: the string we want to return its length
 * Return: int the length of the string
 */
int _strlen(char *s)
{
	int strsize = strlen(s);

	return (strsize);
}
